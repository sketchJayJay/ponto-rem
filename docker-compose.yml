services:
  ponto-rem:
    build: .
    container_name: ponto-rem
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: "troque-essa-chave"
      ADMIN_USER: "admin"
      ADMIN_PASSWORD: "admin123"
      DATA_DIR: "/data"
    volumes:
      - ./data:/data
      - ./uploads:/app/static/uploads
    restart: unless-stopped
